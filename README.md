Насибулин Анвар Ринатович
M60-123БВ-25
Python

## Запуск проекта

```bash
python -m src.db
```

## Запуск тестов

```bash
python -m unittest discover
```

## Что умеет проект

- добавление мемов в память
- поиск мемов по одному или нескольким полям
- обновление существующих записей
- удаление мемов по ID
- просмотр всех записей

## Структура проекта

```text
src/db/backend/record.py  - класс записи
src/db/backend/table.py   - работа со списком записей
src/db/backend/memory.py  - класс базы данных в памяти
src/db/tui.py             - текстовое меню
tests/test_memory.py      - автоматические тесты
```

## Коротко о реализации

Каждый мем хранится как объект `Record`, внутри таблицы записи лежат в обычном списке.
Класс `Memory` используется для работы с таблицей и выполнения основных операций.
